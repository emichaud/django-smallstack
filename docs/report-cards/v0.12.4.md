# SmallStack Quality Report Card — v0.12.4

| | |
|---|---|
| **Project** | SmallStack (base) |
| **Version** | 0.12.4 |
| **Branch / commit** | `feat/runbook` @ `2251250` |
| **Date** | 2026-07-12 |
| **Reviewer** | Integration test harness (`test_smallstack_base`) — Claude Code |
| **Scope** | Merge-readiness of `feat/runbook` (runbook app + supporting changes, 12 commits) vs `main` (`7816b1c`) |

## Overall: **A−**  ·  ✅ Recommend merge
The runbook dynamic-document system is complete and clean. The suite is green (**1475 pass / 0 fail**,
80% coverage), there are **0 known vulnerabilities**, and **0 open findings**. Every issue raised during
review was fixed and independently re-verified this cycle: the stored-XSS **BLOCKER** (F4), the
loose-doc export gap (F5), and the API-discoverability observation. Doctors are green (api 11✓ / mcp 7✓
/ search 5✓) and migrations are clean. **Merge-ready.** A− (not A) reflects a security-critical class
having recurred in new code (now closed via a shared hardened renderer) and coverage at 80%.

## Scorecard
| # | Area | Grade | Trend | Evidence |
|---|------|:-----:|:-----:|----------|
| 1 | Security | **A−** | → | pip-audit **0 vulns**; 0 open findings; CSP/HSTS/secure-cookies/token-hashing intact; authz verified (read-only→403, non-owner→403); spoof-resistant client-IP; stored-XSS BLOCKER found + fixed + re-verified |
| 2 | Code Quality | **A−** | → | `ruff` clean; de-dup refactors (shared client-IP resolver, shared markdown renderer); N+1 removed in CLI; typed/docstringed new surfaces |
| 3 | Testing & Coverage | **B+** | ↑ | **1475 passed / 0 failed**; coverage **80%** (~18 runbook test files). 80% = B+ band |
| 4 | Documentation & Skills | **A** | ↑ | 2 new skills + `index.json`; CLI docs verified verbatim; runbook REST endpoints now discoverable (OpenAPI schema + Swagger/ReDoc + api_doctor "Custom endpoints") |
| 5 | Architecture & Design | **A** | → | one `service.put_document` write-path across web/REST/MCP/CLI; domain signals; immutable version snapshots; clean permission/retention modules |
| 6 | Operability & Release | **A−** | ↑ | doctors green (api **11✓** / mcp 7✓ / search 5✓); migrations clean; reproducible `make setup`; `run_retention`; version synced |
| 7 | Accessibility & Theming | **A−** | ↑ | **Verified** — runbook doc/read/dashboard pages screenshotted across the 5 palettes (faithful profile-switch). Orange dark **and** light are crisp (no muddy/brown surfaces — the classic hardcoded-hex failure); high-contrast + purple clean; code/tables/blockquotes render with good contrast; no invisible elements. Screenshots in `screenshots/`. |

_Trend vs the prior card (`0c7c32e`, A−): held at A−, with ↑ where the API-discoverability fix, the
runbook-pages palette verification, and +1 test landed._

## Changes this cycle (by category)

### 🔒 Security (2)
- **Fix stored XSS in runbook document markdown rendering (BLOCKER)** — shared `harden_markdown_renderer`; dropped `md_in_html`/`attr_list`. (`69fbc87`)
- **Spoof-resistant real client-IP behind kamal-proxy** — rightmost-trusted `X-Forwarded-For`, gated on `TRUST_PROXY_HEADERS`.

### ✨ New Features (2)
- **Integrate the runbook app** — Runbook/Section/Document/Version/Image models, ownership + public/private, immutable versioning, retention, subscriptions. (`feat(runbook)…`)
- **Expand runbook CLI, REST, and dashboard surfaces** — `rb` CLI, `/…/runbook/api/…` REST, dashboard + stats.

### ⬆️ Enhancements (3)
- **Eliminate N+1 queries** in runbook CLI `ls`/`toc`/`sections`. (`perf(runbook cli)…`)
- **api_doctor surfaces hand-registered (`register_api_path`) endpoints** — runbook REST now visible in the "Custom endpoints" check + OpenAPI schema. (`2251250`)
- **Align activity filter Apply/Reset buttons** with the control row. (`mcp(activity)…`)

### 🐛 Bug Fixes (1)
- **Include section-less ("loose") docs in ZIP export (F5)** — export dropped section-less docs; now included at the runbook root. (`0c7c32e`)

### ♻️ Refactors (1)
- **Share one proxy-aware client-IP resolver** across activity logging + axes. (`activity+axes…`)

### 📝 Docs & Skills (1)
- **Finish runbook integration** — `rb` CLI + `runbook-documents` skills, index entries, docstrings. (`chore(runbook)…`)

### 🔧 Chore / Infra (1)
- Silence the axes INFO startup banner in dev. (`chore(logging)…`)

## Findings

### Resolved this cycle
| ID | Sev | Summary | Fixed in |
|----|-----|---------|----------|
| F4 | **BLOCKER** | Stored XSS in runbook document rendering | `69fbc87` — shared hardened renderer; re-verified (payload escaped, no live `<script>`) |
| F5 | MINOR | ZIP export omitted section-less docs | `0c7c32e` — loose docs included at runbook root; re-verified (`loose.md` in ZIP) |
| — | Obs. | Runbook REST endpoints not discoverable (api_doctor/OpenAPI) | `2251250` — api_doctor "Custom endpoints" (14) + 14 paths / 23 ops in OpenAPI schema; Swagger/ReDoc 200 |

### Open
| ID | Sev | Summary | Where |
|----|-----|---------|-------|
| _none_ | | | |

## Evidence
- **Tests:** 1475 passed / 0 failed · coverage **80%**
- **Lint:** `ruff` clean · **Security:** pip-audit **0** known vulns
- **Doctors:** api 11✓ · mcp 7✓ · search 5✓ · **Migrations:** clean
- **Theming:** verified — runbook pages render correctly across the 5 palettes × light/dark (screenshot matrix); no muddy surfaces, good contrast

## Merge recommendation
**Merge `feat/runbook` → `main`.** 0 open findings, 0 vulns, all reviewed issues fixed and re-verified,
full suite green on a fresh clone, and the new runbook pages verified correct across all palettes. No
blockers, no outstanding follow-ups.

## Methodology
Snapshot `2251250`, cloned fresh via the test harness. Metrics from `ruff`, `uv run pytest -q`,
`uvx pip-audit`, the `*_doctor` commands, `makemigrations --check`. Change taxonomy from
`git log 7816b1c..2251250`. Findings (F4, F5, observation) from the harness `OPEN_ISSUES.md` +
`results/2026-07-12_*` rounds, each verified end-to-end against a running server. Grades per the rubric
in [`docs/skills/report-card.md`](../skills/report-card.md).

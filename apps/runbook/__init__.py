# App config (RunbookConfig) is auto-discovered from apps.py.
# NB: the app *label* stays "smallstack_runbook" (see apps.py) so existing
# migrations and downstream tables keep working after the move into apps/.
